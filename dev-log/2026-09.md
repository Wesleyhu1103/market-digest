# Dev log — September 2026

## 2026-09-16 — Restore site-root data paths for archive pages
- What: `mdSitePath()` is global again; archive manifest, static FRED fallback, and scoreboard archive reads resolve from the site root, and asset sync now cache-busts archive snapshots. assetVersion -> 20260916.
- Why: archive pages live under `/archive/`, so relative `archive/...` and `fred-data.json` fetches hit missing nested paths and broke archive navigation/GitHub Pages macro data/past press verdicts.
- Redo of: 2026-09-15
- Gotcha: shared JS changes must update `../js`/`../css` cache-bust tags in archives, not just `docs/index.html`.
