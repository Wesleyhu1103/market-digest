## 2026-09-02 — Root archive runtime data fetches
- What: added `mdSitePath()`, routed archive manifest/FRED/scoreboard archive fetches through it, repaired old inline archive URLs, bumped `assetVersion`, and taught config sync to update archive snapshots.
- Why: archive pages under `/archive/` resolved static data and prior archive reads under missing nested paths, blanking archive lists, macro fallback data, and press tags.
- Gotcha: run `sync_site_config.py` after JS/CSS edits; archive snapshots need fresh `?v=` tags too.
