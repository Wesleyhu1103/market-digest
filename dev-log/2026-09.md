# Dev log — September 2026

## 2026-09-14 — Guard manual publish from same-day overwrite
- What: `publish-digest.yml` now skips manual regeneration when the digest is already current and preserves committed `incoming/new-main.html` instead of deleting/overwriting it.
- Why: manual dispatch on a current edition regenerated and spliced a new same-day `<main>` while `publish_digest.py` skipped archiving, losing the previous edition snapshot.
- Gotcha: manual dispatch without an incoming file now only runs catch-up generation when `digest_stale.py` says the live checkout is stale.
