## 2026-09-19 — Restore archive root-path runtime fetches
- What: `mdSitePath()` is global again, archive/runtime FRED and verdict fetches use it, the contract rejects direct relative fetches, and `sync_site_config.py` cache-busts externalized archives. assetVersion -> 20260919.
- Why: archived pages resolved manifest/FRED/verdict archive reads under `/archive/...`, breaking archive lists, macro charts, and historical verdict rows.
- Redo of: 2026-07-23
