# Dev log — September 2026

## 2026-09-13 — Restore archive-safe runtime data paths
- What: `mdSitePath()` is global again; archive/FRED runtime fetches use it, archive snapshots cache-bust to assetVersion 20260913, and the contract rejects direct `fetch('archive/...')` / `loadFred('fred-data.json')` in shared JS.
- Why: archive pages resolved FRED fallbacks and prior digest consensus under `/archive/`, breaking macro charts on GitHub Pages and archive scoreboard press leans.
- Redo of: 2026-07-23
- Gotcha: sync asset versions across `docs/archive/*.html` whenever shared JS changes, or old snapshots keep loading stale code.
