## 2026-09-26 — Restore archive runtime root paths
- What: `mdSitePath()` is back in shared config, archive/FRED/verdict fetches use it, and sync now cache-busts externalized archive snapshots.
- Why: Sep externalized archives resolved runtime data under `/archive/`, breaking macro fallback data, archive links, and historical verdict press reads.
- Gotcha: shared JS fixes need an assetVersion bump plus archive tag sync or cached archive pages keep loading the broken scripts.
