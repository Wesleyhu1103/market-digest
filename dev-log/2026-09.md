# Dev log — September 2026

## 2026-09-25 — Restore archive runtime data root paths
- What: `mdSitePath()` now resolves site-root data paths for archive pages; archive manifest, static FRED fallback, and verdict press-history fetches use it, with assetVersion 20260925 synced through externalized archives.
- Why: the Sep 24 archive loaded shared JS but resolved runtime data as `archive/fred-data.json`, `archive/archive/manifest.json`, and nested prior archive URLs, breaking macro charts/archive navigation/press tags on archived pages.
- Gotcha: cache-bust sync has to include `docs/archive/*.html`, not just live `index.html` and `admin.html`.
