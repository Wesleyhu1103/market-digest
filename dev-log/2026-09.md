## 2026-09-23 — Fix archive runtime data paths
- What: restored global `mdSitePath()` usage for archive/FRED runtime fetches, made `sync_site_config.py` cache-bust externalized archive assets, and added contract/tests for relative-fetch regressions.
- Why: archive pages loaded shared JS from `../js`, but relative fetches still resolved under `/archive/`, breaking macro fallbacks, archive links, and prior-day press reads.
- Gotcha: JS/CSS asset bumps must update externalized archives too, or returning archive visitors can keep stale shared JS.
