## 2026-09-12 — Restore site-root archive runtime fetches
- What: `mdSitePath()` now anchors shared archive/FRED URLs at the site root; archive manifest, macro FRED fallback, and verdict press reads use it, with archive cache-bust sync + tests.
- Why: archived pages resolved `fred-data.json` and `archive/*.html` under `/archive/`, breaking macro charts, archive lists, and historical press leans on archive views.
- Redo of: 2026-08-28
- Gotcha: bump `assetVersion` and sync externalized archives whenever shared JS changes, or cached archive pages keep the old fetch paths.
