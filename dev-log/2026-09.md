# Dev log — September 2026

## 2026-09-20 — Root-safe archive runtime paths + archive cache-bust sync
- What: `mdSitePath()` now roots archive/static reads; archive nav, FRED fallback, and verdict press fetches use it; `sync_site_config.py` updates archive `../js`/`../css` versions. assetVersion → 20260920.
- Why: archive pages under `/archive/` resolved manifest/FRED/press reads under `/archive/archive` or `/archive/fred-data.json`, and old cache tags kept the fixed JS from loading there.
- Redo of: 2026-07-08
- Gotcha: assetVersion bumps intentionally touch every externalized archive so cached archive JS is refreshed.
