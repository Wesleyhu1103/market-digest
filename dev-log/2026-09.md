# Dev log — September 2026

## 2026-09-24 — Fix archive runtime root paths + cache busts
- What: `mdSitePath()` now resolves archive pages back to the site root; archive manifest, FRED fallback, and scoreboard archive reads use it; assetVersion -> 20260924 and sync now updates externalized archive snapshots.
- Why: archived pages were loading JS but fetching runtime data from `/archive/...`, so archive lists, macro fallbacks, and past-day press reads broke on archived digests.
- Gotcha: cache-bust archive `../js`/`../css` tags whenever shared JS changes, or archived pages can keep running the broken cached bundle.
