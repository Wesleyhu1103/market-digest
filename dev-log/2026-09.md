# Dev log — September 2026

## 2026-09-22 — Restore archive root paths and cache-bust archive snapshots
- What: `mdSitePath()` resolves shared data paths from the site root, archive/FRED/verdict fetches use it, `sync_site_config.py` updates externalized archives, and assetVersion -> 20260922.
- Why: archived pages were requesting `/archive/archive/manifest.json`, `/archive/fred-data.json`, and stale `?v=20260724` JS/CSS, breaking archive navigation, macro fallbacks, and historical verdict press reads.
- Gotcha: cache-bust externalized archives whenever shared JS/CSS changes, not just `index.html` and `admin.html`.
