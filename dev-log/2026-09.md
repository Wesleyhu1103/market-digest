# Dev log — September 2026

## 2026-09-02 — Fix archive runtime root data paths
- What: added shared `mdSitePath()` root resolution, routed archive manifest/FRED/press fetches through it, and cache-busted live/admin/externalized archive pages to assetVersion 20260902.
- Why: archive pages resolved `archive/manifest.json` and `fred-data.json` under `/archive/`, breaking archive navigation, macro charts, and archived press reads despite passing validation.
- Gotcha: sync asset versions across externalized archive snapshots too; otherwise old `?v=` tags can keep serving stale JS on archived pages.
