## 2026-09-01 — Root archive data fetches and cache-bust snapshots
- What: `mdSitePath()` now resolves archive/FRED/scoreboard data from the site root; legacy inline archives use `../` static data paths; archive snapshots get assetVersion sync; validator/tests reject direct archive-relative and static FRED fetches.
- Why: archived pages evaluated `archive/...` and `fred-data.json` from `/archive/`, breaking archive navigation, GitHub Pages macro charts, and historical press reads.
- Redo of: 2026-07-23
