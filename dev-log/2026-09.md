# Dev log — September 2026

## 2026-09-11 — Restore archive runtime data paths
- What: `mdSitePath()`/`mdSiteBasePath()` now route archive manifest, static FRED fallback and scoreboard archive reads from the site root; assetVersion -> 20260911 and `sync_site_config.py` updates externalized archive snapshots too.
- Why: Sep 10 archive pages loaded shared JS but fetched `/archive/archive/manifest.json`, `/archive/fred-data.json` and nested scoreboard HTML, breaking archive navigation/charts/press tags.
- Redo of: 2026-08-28
- Gotcha: old inline June/early-July archives have no external asset tags; validate/scans should focus externalized archives plus shared JS path calls.
