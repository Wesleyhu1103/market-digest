# Dev log - September 2026

## 2026-09-14 — Restore archive root-path fetches
- What: `mdSitePath()` resolves root assets from archive pages; archive drawer, static FRED fallback, and scoreboard press fetches use it. `sync_site_config.py` now cache-busts externalized archives, and the contract rejects archive-relative data fetches.
- Why: Sep 14 publish exposed externalized archive pages resolving `archive/...` and `fred-data.json` under `/archive/`, breaking archive navigation and macro fallbacks.
- Gotcha: old self-contained June/early-July archives still contain inline legacy fetch code; the current externalized path is guarded.
