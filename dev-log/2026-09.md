# Dev log - September 2026

## 2026-09-15 - Restore archive runtime root paths
- What: `mdSitePath()` is global again; archive manifest, static FRED fallback, and scoreboard archive reads resolve from the site root, and `sync_site_config.py` cache-busts externalized archives. assetVersion -> 20260915.
- Why: Sep 15 publish created a new archive while archive pages still fetched `fred-data.json` / `archive/*.html` relative to `/archive/`, breaking macro fallbacks and press tags on GitHub Pages/outage paths.
- Redo of: 2026-08-28
