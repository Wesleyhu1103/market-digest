## 2026-09-22 — Root archive runtime paths + archive cache-bust
- What: `mdSitePath()` now roots archive manifest/FRED/archive-page fetches from the site base; assetVersion -> 20260922 and sync updates externalized archive snapshots too.
- Why: archived pages resolved runtime data under `/archive/archive/` or `/archive/fred-data.json`, breaking archive navigation/history reads and static FRED fallback.
- Gotcha: JS fixes need archive HTML cache-busts, not just `index.html`/`admin.html`, or old archive pages keep loading stale shared assets.
