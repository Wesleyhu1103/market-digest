# Dev log — July 2026

## 2026-07-24 — Rewire feedback pipeline + digest watchdog (both were failing silently) → didn't hold (feedback leg only: the FEEDBACK_ADMIN_SECRET repo secret never got added, so every run stayed red at preflight — flag #26 seen 2x), see 2026-07-30
- What: process-feedback.yml had failed ALL 3 runs it ever had (empty POSTGRES_URL secret, and the DB layer wants SUPABASE_DB_URL) — the admin board never received a single clustered proposal. It now goes through the deployed admin API via new `scripts/call_admin_api.py` (retries w/ backoff, 4xx fail-fast with a pointed hint; sole credential FEEDBACK_ADMIN_SECRET), preflights the secret loudly (maintenance flag + ::error with setup steps), refreshes one open `feedback-review` issue instead of stacking duplicates, and flags any failure to the maintenance board. digest-watchdog.yml: the red run on 7/24 was a QUEUED run cancelled inside its own concurrency group with the digest already published (pure infra noise) — dropped the group (publish-digest's own group dedupes dispatches), trimmed 29 crons/day → 17 (11:00–15:00 UTC window), added timeout-minutes + 3× gh retries, and the 13:30 UTC alarm re-verifies against live main first (`digest_stale.py --remote`, falls back to local so uncertainty still alarms). cron-watchdog.js self-reports "GITHUB_TOKEN missing"/"dispatch failed" as critical maintenance flags via the _supa upsert (a 503 in Vercel logs is invisible). maintenance-monitors.yml now sweeps the latest run of publish/process-feedback/digest-watchdog daily.
- Why: red must mean signal and green must mean the pipeline actually ran — the watchdog cried wolf while the feedback pipeline rotted quietly for 3 straight weeks.
- Verified: 7 new stdlib tests (tests/test_call_admin_api.py: retry, 401 no-retry, refused-connection, remote stale/current/fallback), yaml + bash -n on every workflow run block, mock-API smoke of both step scripts end-to-end (cluster → issue dedupe/create; stale → dispatch → alarm), node --check.
- Gotcha: FEEDBACK_ADMIN_SECRET must ALSO exist as a GitHub Actions repo secret (same value as the Vercel env var) — until it does, Monday runs red at preflight with instructions instead of the old cryptic 15-second failure.

## 2026-07-09 — Style bare Week Scoreboard table
- What: `.week-scoreboard table` now shares the `.week-table` CSS (width, padding, header band) in `digest.css`; assetVersion → 20260713 + sync.
- Why: the daily `<main>` emits a plain `<table>` inside `.week-scoreboard` (no `week-table` class), so the scoreboard rendered as an unstyled cramped table.
- Gotcha: fixed at the template/CSS layer on purpose — generated daily content keeps emitting the bare `<table>` and stays contract-clean.

## 2026-07-09 — Streamline macro charts: VIX axis + credit daily ranges → didn't hold, see 2026-07-09 (2s10s alignment)
- What: Credit Spreads drops misleading 1D button (FRED OAS is daily close only); defaults to 1W with source line "daily close · through …". Market Stress intraday (1D/1W live) is VIX-only — single right axis, no 2s10s line that compressed to 0–2 bps. 2s10s returns on 1M/1Y FRED view only. Bumped assetVersion → 20260711.
- Why: 1D on credit showed Jul 2–7 flat lines (not intraday); live 2s10s est. used stale daily 2Y on VIX timeline so the right axis read 0–2 bps next to VIX ~17.
- Gotcha: credit still won't move intraday — that's the data feed, not publish. Honest ranges + labeling is the fix.

## 2026-07-09 — Align 2s10s on Market Stress 1W+ (FRED daily on right axis)
- What: 1W/1M/1Y live shows VIX (left) + FRED daily 2s10s stepped on right bps axis; 1D stays VIX-only. Dropped live ^TNX−2Y est. that plotted ~0–2 bps. `renderChart` now deletes stale `y2` scale on range switch. Redo of: 2026-07-09 (streamline entry). assetVersion → 20260712.
- Why: intraday 10Y minus stale daily 2Y made the spread line sit on the wrong scale; user wanted both series on 1W with aligned units.
- Gotcha: 2s10s on live ranges is daily FRED (stepped), not tick-by-tick — source line says "2s10s daily through …".

## 2026-07-08 — Wire assetVersion into cache-bust tags
- What: `scripts/sync_site_config.py` now also rewrites `?v=<assetVersion>` on local `js/`/`css/` tags in `index.html` + `admin.html` (was 15 hand-maintained `?v=` copies, orphaned from the `assetVersion` field). Bumped `assetVersion` 20260709 → 20260710 so the chart fixes reach returning visitors.
- Why: the `assetVersion` in `site-config.json` drove nothing for HTML, so a JS/CSS change shipped stale to cached clients unless all 15 tags were edited by hand.
- Gotcha: edit `assetVersion` then run `sync_site_config.py`; the regex only touches `src|href="(js|css)/…?v=…"` (CDN/font URLs untouched).

## 2026-07-08 — Fix chart theme-switch race + credit chart local/offline render
- What: `_initAllCharts` is now synchronous (dropped a dead `await fetch('fred-data.json')` whose `fred`/`brent`/`creditData` results were unused) — kills the "Canvas is already in use" race between the load event and theme init; `charts-macro.js` `refreshFredMacro` now falls back to static `fred-data.json` when `/api/fred-data` is unreachable. Verified in headless Chromium: 7/7 charts render across all 4 themes, 0 JS errors.
- Why: async gap in `_initAllCharts` let two concurrent runs create a 2nd Chart on the same canvas (intermittent blank/duplicate charts on load/theme switch); Credit Spreads has no live fallback, so it never rendered on local preview (API 404s off Vercel).
- Gotcha: Credit OAS is still daily FRED — fallback shows the last published snapshot, not intraday. `/api/fred-data` still 404s on localhost (expected); the fallback is what makes credit/stress draw there.

## 2026-07-08 — admin.html reads shared origin + full wiring check
- What: `docs/admin.html` now loads `js/site-config.js` and reads `SiteConfig.vercelOrigin` (defensive literal fallback) instead of a hardcoded Vercel URL; ran end-to-end wiring check (imports, JS load order, CI script refs, JSON validity, asset version).
- Why: it was the last live file with a hardcoded origin, missed by Phase 2 since it doesn't share the digest shell's script chain.
- Gotcha: admin `mdUsesRemoteApi()` treats localhost as remote (no local API server) — kept that; only the origin literal changed. Also fixed a stale `test_freshness_filter` (FeedItem gained `published_label`).

## 2026-07-08 — Route agents (v0/Claude/Cursor) to the contract
- What: rewrote `AGENTS.md` top as "where to work" routing table pointing daily-content and structural edits at `contracts/digest-main.json`; added contract-check command + CI note; README v0 section + AGENT_HANDOFF/SKILL now reference the contract. Added `community-votes`/`cv-board` validate rules (now 14 main + 6 static).
- Why: v0 reads `AGENTS.md` at repo root; it was Cursor-framed and never mentioned the contract, so v0 could freehand `index.html`.
- Gotcha: `AGENTS.md` no longer hardcodes a check count (was stale "12"); sync-agent-branches CI blocks contract drift before merge.

## 2026-07-08 — Phase 3: single-source digest contract
- What: `contracts/digest-main.json` drives `generate_digest.py` system prompt and `validate_digest.py` regex checks; `digest_contracts.py --check` in CI.
- Why: structural rules lived in three places (AGENT_HANDOFF, inline SYSTEM, validate RULES) — contract changes had to be triple-edited.
- Gotcha: edit `contracts/digest-main.json` first; keep AGENT_HANDOFF examples aligned for human/agent readers.

## 2026-07-08 — Phase 2: shared site-config + dedupe live helpers
- What: `docs/site-config.json` drives FRED series, repair chart heights, Vercel origin; `scripts/digest_date.py` + `docs/js/digest-date.js` unify edition parsing; verdict updater uses `MarketFeed`; `sync_site_config.py` generates `site-config.js`.
- Why: Phase 1 split files but constants/proxy logic still duplicated — agents editing FRED or repair rules had to touch 3+ places.
- Gotcha: edit `site-config.json` then run `python3 scripts/sync_site_config.py` before commit; Python scripts need `scripts/` on path for `site_config` imports.

## 2026-07-08 — Phase 1: extract CSS/JS from index.html
- What: moved ~2,900 lines of inline CSS/JS into `docs/css/digest.css` and 11 `docs/js/*.js` modules; slimmed `index.html` to shell + `<main>`; `validate_digest.py` now parses external scripts.
- Why: monolith made chart/verdict debugging slow; pure file move preserves behavior while enabling targeted edits.
- Gotcha: script load order in `index.html` is the contract — don't reorder without checking globals (`MarketFeed`, `_initAllCharts`).

## 2026-07-07 — Fix 2s10s stepped chart (interpolate live 10Y)
- What: 2s10s now uses linear ^TNX interpolation on the VIX timeline (not one value per day); legend/tooltip say "2s10s (est.)" with 2Y FRED as-of in source line.
- Why: daily bucketing made the spread look like flat FRED stairsteps; live 10Y minus daily 2Y should move intraday with the 10Y.
- Gotcha: official FRED T10Y2Y close (e.g. 35 bps Jul 6) can differ slightly from live est. when 10Y moves after the 2Y print.

## 2026-07-07 — Fix macro live fetch fallbacks (VIX/treasury)
- What: MarketFeed calls no longer fail as a batch (per-series catch); empty live series falls back cleanly; `/api/fred-data` tries FRED CSV when no API key instead of static-only; credit/stress always init on boot.
- Why: one failed ^TNX quote rejected all three stress fetches → stale FRED path with "SOURCE: FRED / CBOE" through Jul 3.
- Gotcha: live VIX headline should read ~16.x and source "Cboe / FRED · Live · …" after deploy.

## 2026-07-07 — Live FRED API + VIX for macro charts → didn't hold, see 2026-07-07
- What: added `/api/fred-data` (FRED API refresh, 15m cache) for credit/stress between publishes; VIX/2s10s now live from ^VIX/^TNX; credit source shows last FRED observation date.
- Why: polling static `fred-data.json` only re-read the publish-time snapshot (HY OAS stuck Jul 3).
- Gotcha: credit OAS is still daily FRED — live API gets the latest published close, not intraday.
- Redo of: 2026-07-07 (live treasury tooltips entry — partial; credit/stress still stale)

## 2026-07-07 — Live treasury charts + precise macro tooltips → didn't hold, see 2026-07-07
- What: treasury 10Y/30Y now pull from Cboe (^TNX/^TYX) via MarketFeed with 60s auto-refresh; tooltips show CNBC-style dates (MM/DD/YYYY, time on intraday); Brent/credit/stress get the same tooltip treatment; FRED fetch window bumped to 400d for real 1Y history.
- Why: macro charts were stuck on stale FRED close (Jul 2) and 1Y hover showed yields with no date.
- Gotcha: 2Y still forward-fills from FRED until a live 2Y feed exists; headline 10Y is live Cboe.

## 2026-07-06 — Fix silent daily publish routing (Vercel cron backup was dead)
- What: found the digest stayed stale until 10:23am ET; only a delayed GitHub `digest-watchdog` cron rescued it. Root cause: `GITHUB_TOKEN` was never set in Vercel production, so the reliable Vercel cron `/api/cron-watchdog` (8am ET) detected staleness, returned 503, and never dispatched — publishing has been relying 100% on GitHub's flaky cron. Added `console.log/error` to cron-watchdog for Vercel-log visibility, a loud red-run alarm in `digest-watchdog.yml` when still stale past 09:30 ET, fixed `digest_stale.py` crash on Python <3.10 (`from __future__ import annotations`), and corrected the README env-var table (GITHUB_TOKEN is required, CRON_SECRET is not auto-set).
- Why: schedulers fail silently; the backup path was misconfigured and nothing alerted, so a stale digest was only caught by the reader.
- Gotcha: the actual fix is out-of-repo — set `GITHUB_TOKEN` (PAT, `actions:write`) and `CRON_SECRET` in Vercel Production env. Verify next morning that a `workflow_dispatch` publish run appears ~12:00 UTC (from Vercel), not just the ~11:00 UTC GitHub watchdog.

## 2026-07-02 — Add outcome tracking to dev-log convention
- What: agents now flag prior entries with "→ didn't hold" when a change redoes an earlier fix
- Why: makes the log grade itself — unflagged entries held, flagged ones are the bad list

## 2026-07-02 — Add Obsidian logging guide
- What: added `dev-log/logging-guide.md` mapping each digest section to what to log by hand in market-notes
- Why: makes the daily Verdict leans + theme notes routine concrete instead of ad hoc

## 2026-07-02 — Set up dev-log + agent auto-logging
- What: added `dev-log/` folder, `CLAUDE.md`, and a logging rule in `AGENTS.md`
- Why: agents now append a short entry after every fix so past decisions are readable in Obsidian instead of re-solved
- Gotcha: open the repo folder itself as the Obsidian vault (not a copy) or edits won't sync

## 2026-07-20 — Range-consistent chart headlines, per-day 1W labels, credit dual axis → didn't hold (credit dual axis only), see 2026-07-20 (Δ rework)
- What: live-card change readouts (S&P/Nasdaq/Dow/10Y, VIX, 10Y card, Brent, credit) now baseline against the SELECTED range (prev close on 1D, window start + "past week/month/year" suffix otherwise) and the line color follows; 1W x-axis labels are one-per-trading-day centered mid-day (ET-pinned) which also kills the first-tick overflow that shrank the plot's left edge; Credit Spreads got separate HY(left)/IG(right) axes with color-coded ticks (shared 50–300 bps axis rendered both as flat lines); 2s10s + IG use navy `--anchor` (ECON theme's `--accent` red was identical to the VIX/HY line); equities card no longer leaks Chart instances on range/symbol switches. assetVersion → 20260720. Verified in headless Chromium with stubbed Yahoo/FRED data: 4/4 themes, 8 charts, 0 JS errors.
- Why: 1W header said "+4.78 (+0.06%)" in green over a chart falling 0.5% (daily change vs weekly window), duplicate "Tue, 7/14" labels came from i%step labeling, and credit daily moves were invisible at 2px/5bps.
- Gotcha: credit/2s10s remain FRED daily closes (source line says "through <date>"); on Mondays they can trail by 1–3 days — that's the feed's publishing lag, not a chart bug.

## 2026-07-20 — Credit Spreads: dual axes → change-in-bps on one shared axis
- What: `initCredit` now plots HY and IG as change in bps since each series' first close in the selected window on ONE shared signed-bps axis; legend carries the at-rest levels ("HY OAS · 271 bps"), source line names the rebase date ("change since <date>"), tooltips show level + signed change. Axis is a custom integer-step lattice (≤6 ticks, 0 always a tick) because Chart.js 4.4 maxTicksLimit respacing silently drops the 0 tick and half-bp-mislabels rounded ticks on quiet-week spans — caught by adversarial review before ship, reproduced against real chart.umd. Treasury chart drops all-null series via `mdDropEmptySeries` so 1D no longer legends a phantom 2Y (FRED 2Y has no same-day print). assetVersion → 20260721.
- Why: the dual auto-scaled axes shipped earlier today stretched both series to fill the plot, so HY (~270) and IG (~78) overlapped and crossed as if identical — reader-reported. Two same-unit series ~200 bps apart mislead on any shared-level or dual-level axis; the Δ view is the desk-standard "spread change" read.
- Redo of: 2026-07-20 (credit dual axis)
- Gotcha: the y axis is CHANGE, not level — levels live in the tooltip and headline. Don't "fix" it back to levels; that path is flat lines (shared axis) or the overlap illusion (dual axes), both already rejected.

## 2026-07-20 — Diversify feed roster beyond Bloomberg + on-demand feed check
- What: added CNBC Finance/Economy, MarketWatch, WSJ Markets (Dow Jones public RSS), Yahoo Finance, FT Home to `feed_sources.py`; contract gained a source-diversity content rule (rotate outlets, Bloomberg ≤ ~2/3 of a section's links when alternatives exist); `python3 scripts/feed_sources.py` now prints a status table and `.github/workflows/feed-check.yml` (workflow_dispatch) runs it from a runner with real egress.
- Why: production Source Coverage showed all feeds healthy — Bloomberg dominance was pool composition (4/7 Bloomberg feeds; CNBC Top News only 1–9 fresh items/day vs Bloomberg's 45–75), not a scraping failure.
- Gotcha: agent sandboxes can't reach news CDNs (proxy 403s), so feed URLs can only be truth-tested via the feed-check workflow or the next morning's Source Coverage block; failed feeds degrade gracefully to a "Failed" line there.
- Verified: feed-check run #1 on this branch — 13/13 reachable; fresh items WSJ 15, Yahoo 25, MarketWatch 10, FT 10, CNBC Finance 3 vs Bloomberg 48 (CNBC Economy + Money Stuff stale-but-healthy that hour).

## 2026-07-22 — Week Scoreboard live-resolves (was permanently "Pending") → didn't hold, see 2026-08-12
- What: `verdict-updater.js` now fills `#weekTbody`: the 5-layer scorers were parameterized (`scoreAllFrom(snapshot)`) so past days score from Yahoo daily close-to-close bars, today tracks the live quote cache ("· live") and finalizes after 4pm ET, future days stay Pending; each resolved cell carries that day's press lean parsed from the archived digest's `consensusData` (today's from the DOM), and the `#ssRight/#ssWrong/#ssMixed/#ssRate` summary finally updates. Refresh: with the card scoring cadence intra-day plus a 15-min daily-bar repull that also runs after hours. assetVersion → 20260722.
- Why: the scoreboard table had ids no JS ever wrote — every cell said Pending forever, even for past days; reader-reported.
- Gotcha: cells resolve deterministically from history on every load (no storage) — a day with missing Yahoo bars or a missing archive page degrades to Pending/no-press-tag rather than guessing. Week rows are parsed from the generated `<main>` table text ("Mon 7/20"), so the daily generation must keep that day-cell format.

## 2026-07-23 — Vercel Web Analytics on the digest (static-site snippet)
- What: `index.html` shell now injects `/_vercel/insights/script.js` (defer) behind a hostname guard — skips `*.github.io`/localhost where the endpoint doesn't exist, loads on the Vercel origin. Web Analytics enabled in the dashboard by hand; daily visitors/pageviews appear under project → Analytics.
- Why: no readership data existed; the dashboard's Get Started shows the Next.js npm path, which doesn't apply to this no-build static site — the script tag is the correct integration.
- Gotcha: ad-blockers eat the script (undercounts), and GitHub Pages traffic is invisible to Vercel by design. Future archive copies of index.html inherit the snippet automatically. If a Vercel Agent PR appears adding `@vercel/analytics` via npm, close it — that path can't work here.
## 2026-07-23 — Restore archive asset and interaction-date guards → didn't hold, see 2026-08-12
- What: archive snapshots rewrite `js/`/`css/` to `../js`/`../css`, validation fails missing local assets, shared interaction JS reads `DigestDate.editionIso()`, and assetVersion → 20260723.
- Why: Jul 8-22 archives loaded missing `/archive/js|css` assets and quiz/feedback/verdict submissions still posted under Jul 8.
- Gotcha: keep publisher rewrite, validator local-asset check, and no-hardcoded-date contract rule together; artifact repairs touched existing externalized archives.

## 2026-07-23 — Site Maintenance board: monitors feed flags into admin.html → didn't hold (queue-cleanup leg only: closing the contiguous #31–59 left #26–30/#62–63 open, and daily cursor re-files #65–70 kept arriving — bot-pr flag #8 recurred 6x), see 2026-07-30 (cron fail-closed)
- What: new `maintenance_flags` table + `/api/maintenance` (POST for monitors via `MAINTENANCE_TOKEN`/`CRON_SECRET`, GET/PATCH under the admin bearer); admin.html grew a third "Site Maintenance" tab (open/acked/resolved, Ack/Resolve/Reopen, "Copy for Claude" debug-brief button). Feeders: digest-watchdog's stale alarm, feed-check failures, and a new daily `maintenance-monitors.yml` (mirrors open bot PRs, flags a failed publish run). Upserts dedupe by key; a recurrence of a resolved flag reopens it and bumps seen_count — the "didn't hold" signal, surfaced as a red badge.
- Why: cursor's daily bug-investigation had 30 unmerged PRs nobody saw (the archive-asset bug sat broken for 2 weeks); monitor output now lands on the admin page instead of piling up in the PR queue. Merged cursor PR #60 (archive `../js` rewrite + edition-date interaction fix) and closed #31–59 as superseded the same day.
- Gotcha: requires the `MAINTENANCE_TOKEN` secret set in BOTH GitHub repo secrets and Vercel Production env (README table updated); feeders no-op politely without it. Flag POST failures never fail the calling monitor.

## 2026-07-23 — Deploy failure: 13th serverless function hit the Vercel Hobby cap
- What: production deploy of the maintenance board failed — Vercel Hobby allows 12 serverless functions per deployment and `api/maintenance.js` was the 13th. Folded `admin-process-feedback` into `admin-proposals` as `{action:"process"}` (admin.html cluster button updated), deleted the extra file, added `tests/test_api_function_limit.py` so CI trips before the deploy does.
- Why: the function count was an invisible budget; nothing warned when a new endpoint crossed it, and the failed deploy silently left the old site live.
- Gotcha: any future endpoint must fit in 12 — extend an existing handler with an `action` field (see admin-proposals) instead of adding a file, or upgrade the Vercel plan.

## 2026-07-23 — Narrative Threads: fix frozen all-mixed rail + reframe bull/bear/mixed verdict rules
- What: the sidebar dots rendered ONCE at load from the pre-resolution DOM (verdict-updater resolves cells async) and never re-read — every day showed mixed forever; text sniffing also inverted strength ("✓ Confirmed"/"✗ Failed" contain neither "bull" nor "bear" → mixed, while "Fragile - leans bull" read as full bull) and future days sniffed as mixed. Now verdict-updater stamps `data-verdict` on scored cells / `data-lean` on cards and dispatches `mktdig:verdicts-updated`; digest-init re-reads on the event, dedupes today against its table row, and renders bull/bear/lean-bull/lean-bear/mixed/pending (lean = soft fill + ring, pending = dashed). Verdict rules reframed in BOTH classifiers (cellVerdict + toVerdict): confirmed = 4+ layers OR 3+ unopposed (was 4+ only); lean = net 2 layers (3v1, 2v0); mixed = |net| ≤ 1 (was "anything short of supermajority", which swallowed 3R/1G and 2R/0G days). assetVersion → 20260724.
- Why: reader-reported all-grey rail on a clear bond-selloff day; layers 4-5 abstain most days by design, so 4/5 agreement made bull/bear near-unreachable and mixed the structural default at both levels.
- Verified: headless Chromium + stubbed MarketFeed week (quiet Mon → mixed, 5R Tue rout → bear, 3R/0G Wed → bear under new rules, live Thu 4R bonds + net-2 AI → lean-bear, Fri → pending): rail/cells/cards/summary/drawer all assert green, 0 JS errors.
- Gotcha: the week-summary line counts leans in "mixed" on purpose (its labels say "confirmed"); don't promote leans into the confirmed counts.

## 2026-07-23 — Settle undated-feed-item policy (cursor PRs #53/#64 recurrence)
- What: undated items never count as fresh or toward `MIN_TOTAL_FRESH_ITEMS` (so they can't mask a stale digest — cursor's valid concern), but still ride along as "[undated]"-labeled source material capped at `MAX_UNDATED_PER_FEED=5` when their feed is otherwise fresh; a stale feed drops its undated items; a fully date-less feed gets new status `undated` (items included, 0 fresh) in Source Coverage. `FeedResult` gained `items_undated`; policy locked in by `test_fetch_feed_undated_policy`.
- Why: cursor proposed the blunt version twice (#53 closed, #64 reopened via the maintenance board's bot-PR flag) — rejecting undated outright would black out any feed that omits or mangles pubDate; counting them fresh let stale content satisfy the thin-digest abort gate. Both failure modes are now closed.
- Gotcha: `FeedResult` construction after `items_fresh` must use keyword args (`newest=`) — `items_undated` sits before `newest` in field order.

## 2026-07-30 — Feedback pipeline runs on the already-synced monitor token (flag #26)
- What: `/api/admin-proposals` now also accepts the monitor token (`MAINTENANCE_TOKEN`/`CRON_SECRET` via `x-maintenance-token`; shared `authorizeMonitor` moved to new dep-free `api/_auth.js`) for exactly the pipeline's two ops — POST `{action:"process"}` and GET `?status=pending`; approve/reject/other-filters/raw-feedback stay admin-bearer-only and scope 403s are decided before any DB touch. `call_admin_api.py` gained `--monitor-env` (bearer still preferred when set); `process-feedback.yml` preflight passes on EITHER credential and only reds when both are absent.
- Why: flag #26 recurred because the 7/24 rewire's sole credential was a hand-copied FEEDBACK_ADMIN_SECRET repo secret that never materialized — while the very same failing runs posted maintenance flags fine, proving MAINTENANCE_TOKEN is already synced GitHub↔Vercel. The root cause was the manual cross-dashboard sync step itself, so it's gone: the admin-bearer repo secret is now optional, not required.
- Redo of: 2026-07-24
- Verified: `node tests/test_admin_auth.mjs` (27 checks: credential+scope matrix and real-handler wiring — 401/403 before the DB layer, 502 only after auth+scope pass); 4 new pytest cases pin bearer-preference, fallback header, no-retry 403 hint, both-missing error (33/33 tests pass); yaml + `bash -n` on all 6 workflows; `node --check` on touched api files. Acceptance PASSED 7/30 15:31 UTC: with the 1ead216 Vercel prod deploy READY, `workflow_dispatch` run #6 went green — preflight ::notice'd the monitor fallback, POST `{action:"process"}` → 200 (1 feedback row clustered → proposal #2 [features]), GET `?status=pending` → 200, review issue #71 opened. First green run in this pipeline's history (runs #1–5 all red).
- Gotcha: the Supabase project visible to agent MCP tooling (`conjziylfpvuuwkmcqsh`) is EMPTY and is NOT production — prod is the Vercel-Marketplace-provisioned DB (`POSTGRES_URL`) reachable only through the deployed API, so verify prod via workflow run logs or `/api/health`, never `execute_sql`. (Found paused/INACTIVE during diagnosis and unpaused to rule it out; it will harmlessly auto-pause again.)
- Gotcha 2: run #6 exposed that the repo never had the `feedback-review` label — `gh issue create --label` failed, the workflow's fallback created #71 UNLABELED, and the next run's dedupe-by-label would have stacked a duplicate. Fixed out-of-repo: adding the label to #71 via the issues API auto-created it, so future runs create/refresh labeled issues; no workflow change needed.

## 2026-07-30 — Cron-watchdog fails closed + bot-PR queue actually cleared (flag #8)
- What: `/api/cron-watchdog` now rejects when `CRON_SECRET` is unset — dep-free `authorizeCronBearer` joins `api/_auth.js`, and the handler turns the unset state into a 503 + critical `cron-watchdog-no-cron-secret` flag (mirrors the GITHUB_TOKEN-missing pattern; a bad/absent bearer with the secret set stays a quiet 401 — public probes are noise, not signal). README upgrades CRON_SECRET to required-for-the-endpoint. Closed the 9 duplicate cursor PRs on this finding (#26/#27/#29/#65–70) as superseded, plus #30 (undated policy settled 7/23) and #62/#63 (archive fix merged as #60); #28 (publish stale-overwrite guard) is the one bot PR left open for a real review.
- Why: bot-pr flag #8 recurred 6x because PR #26 sat open since 7/4 — the 7/23 cleanup closed the contiguous range #31–59 and missed older/newer siblings, and cursor re-filed the same fail-open finding daily because the auth asymmetry was never settled in-repo. The finding was valid (fail-open on unset secret), but cursor's blunt 401-when-missing would recreate the 7/6 failure shape as a silently-rejecting backup; fail closed AND loud is the settle. Prod probe before shipping (Vercel MCP fetch, sandbox proxy 403s vercel.app): unauthenticated GET → 401, so CRON_SECRET is set in prod and the live cron is unaffected — behavior only changes under env drift.
- Redo of: 2026-07-23 (bot-PR queue-cleanup leg)
- Verified: `node tests/test_cron_watchdog_auth.mjs` (12 checks: bearer matrix incl. no-prefix + empty-bearer-vs-unset; handler wiring 405→503→401→work with a recording fetch stub proving zero network before auth clears); admin suite 27 checks + 33 pytest still green; `node --check` on both api files.
- Gotcha: closing a bot PR does NOT resolve its board flag — the sweep only upserts flags for OPEN PRs and never auto-resolves. Resolve the `bot-pr-*` flags (#8 and siblings) on the admin page; they stay resolved because a closed PR can't re-trigger the upsert.
