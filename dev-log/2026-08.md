## 2026-08-28 — Archive runtime fetches use site-root paths
- What: Added `mdSitePath()` and routed archive manifest links, macro FRED static data, and verdict archived press reads through it; sync now cache-busts externalized archives.
- Why: Archived pages under `/archive/` fetched `fred-data.json`, `archive/manifest.json`, and prior press reads relative to `/archive/`, blanking macro/archive widgets and degrading scoreboard reads.
- Gotcha: Legacy May/June archives predate the current contract and still fail full `validate_digest.py`; validate the externalized archives for this path.
- Redo of: 2026-07-08 (archive FRED fallback), 2026-07-22 (scoreboard archived press reads)
