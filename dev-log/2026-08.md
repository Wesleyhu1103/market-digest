# Dev log — August 2026

## 2026-08-24 — Fix archive runtime root paths
- What: shared `mdSitePath()` now resolves root assets from `/archive/*`; archive nav, macro FRED fallback, and verdict press reads use it. assetVersion -> 20260824 + archive cache-bust sync.
- Why: externalized archive pages loaded JS/CSS, but runtime fetches still targeted `archive/archive/*` or `archive/fred-data.json`, blanking archive nav, macro charts, and press tags.
- Gotcha: `sync_site_config.py` must update `../js`/`../css` tags inside archive snapshots too, or archived pages can keep cached stale JS.
