## 2026-08-31 — Restore archive runtime site-root paths
- What: `mdSitePath()` now anchors archive/static data URLs at the site root; archive, macro FRED, and scoreboard archive fetches use it, and asset-version sync updates archive snapshots.
- Why: shared JS loaded from `docs/archive/*.html` was resolving `archive/manifest.json`, `fred-data.json`, and press-read archives under `/archive/`, breaking archived editions.
- Gotcha: bump `assetVersion` for JS fixes and run `sync_site_config.py` so externalized archives pick up the new shared files.
