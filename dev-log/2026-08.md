# Dev log — August 2026

## 2026-08-27 — Fix archive runtime root paths and archive cache-busts
- What: `mdSitePath()` now roots shared data/archive fetches outside `/archive/`; archive.js, verdict-updater, and charts-macro use it; contract guards reject literal relative fetches; `sync_site_config.py` updates archive `?v=` tags; assetVersion -> 20260827.
- Why: archived pages resolved `archive/manifest.json`, previous digest fetches, and GitHub Pages FRED fallback under `/archive/`, breaking archive nav/scoreboard/macro fallback while cached archives kept old JS.
- Redo of: 2026-07-23
- Gotcha: Older pre-extraction inline archives needed direct `../fred-data.json` / `../archive/manifest.json` rewrites because they do not load shared JS.
