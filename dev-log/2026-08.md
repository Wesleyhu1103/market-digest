# Dev log — August 2026

## 2026-08-19 — Fix archive runtime data paths
- What: `config.js` now provides archive-safe root URL helpers; archive nav, macro FRED fallback, verdict archive fetches, and Narrative Threads anchors use them; asset sync now updates archive snapshots too. Redo of: 2026-07-23.
- Why: archived pages loaded JS/CSS but then fetched `fred-data.json` and `archive/...` relative to `/archive/`, breaking macro charts, archive navigation, and historical verdict context.
- Gotcha: cache-busting only live/admin HTML strands archive-only JS fixes; sync archive snapshots whenever static JS changes.
