## 2026-08-16 — Fix archive runtime paths and static-deploy splice guard
- What: archive/runtime helpers now resolve FRED, manifest, and scoreboard archive fetches from the site root; static-only publish deploys skip `publish_digest.py`; assetVersion -> 20260816 across live/admin/archive shells.
- Why: archived pages were fetching `/archive/fred-data.json` and `/archive/archive/...`, breaking macro charts/archive lists/press tags, and a leftover `incoming/new-main.html` could overwrite the live digest during an unrelated static deploy.
- Gotcha: cache-bust sync now includes archive snapshots so old helper URLs do not keep archive runtime bugs alive for returning visitors.
