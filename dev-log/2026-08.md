## 2026-08-22 — Fix archive runtime root paths
- What: shared JS now resolves archive manifest/pages and static FRED fallback from the site root; assetVersion bumped to 20260822 and synced across live/admin/archive shells.
- Why: archived pages were fetching `archive/...` and `fred-data.json` relative to `/archive/`, breaking archive navigation, macro fallback data, and scoreboard press context.
- Gotcha: cache-bust sync must include `../js`/`../css` refs in archive snapshots, or returning archive readers keep stale shared JS.
