# Dev log — August 2026

## 2026-08-18 — Root archive runtime fetches + cache-bust snapshots
- What: `mdSitePath()` roots archive manifest, verdict archive HTML, and static FRED fetches from live/archive pages; `sync_site_config.py` now bumps externalized archive asset tags; assetVersion -> 20260818.
- Why: archived pages resolved shared data under `/archive/archive/` (and GitHub Pages FRED under `/archive/`), breaking archive nav, macro fallback, and historical verdict fills.
- Gotcha: archive snapshots need fresh `?v=` tags too or old cached JS keeps the broken paths.
