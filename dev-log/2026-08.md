## 2026-08-21 — Restore archive runtime root paths
- What: `mdSitePath()` now anchors archive manifest, static FRED fallback, and historical verdict archive fetches at the site root; legacy inline archives got `../` data paths, and `sync_site_config.py` also bumps archive snapshots. assetVersion → 20260821.
- Why: archived pages under `/archive/` loaded JS but fetched missing `/archive/archive/manifest.json`, `/archive/fred-data.json`, and nested consensus snapshots.
- Gotcha: structural validation passed because asset tags resolved; runtime data URLs need focused tests.
- Redo of: 2026-07-23
