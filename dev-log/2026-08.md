# Dev log — August 2026

## 2026-08-30 — Fix archive runtime data paths
- What: `mdSitePath()` now resolves site-root URLs from archived pages; archive manifest links, macro FRED fallback, and verdict press-history fetches use it. Asset sync now updates externalized archive snapshots; assetVersion -> 20260830.
- Why: `/archive/YYYY-MM-DD.html` was requesting `/archive/archive/...` and `/archive/fred-data.json`, breaking archive navigation, macro fallback data, and press tags on archived scoreboards.
- Gotcha: keep the no-relative-runtime-fetch contract rule with `mdSitePath()` and archive cache-busting.
- Redo of: 2026-07-23
