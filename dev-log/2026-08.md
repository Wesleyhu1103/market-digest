## 2026-08-11 — Restore archive root-path helper for shared JS
- What: `mdSitePath()` resolves shared static data/archive URLs from the site root; macro FRED fallback, archive fetches, old inline snapshots, scoreboard date anchoring/year inference, and archive cache-bust tags were repaired.
- Why: Archived digests on GitHub Pages requested `/archive/fred-data.json`/`/archive/archive/YYYY-MM-DD.html`, and archived scoreboards kept advancing to the viewer's real date with Dec/Jan rows mapped to the wrong year.
- Gotcha: `sync_site_config.py` updates live/admin tags only; existing archive snapshots need cache-bust tags repaired when shared JS changes for archives. Validator now fails archive-relative runtime data fetches.
- Redo of: 2026-07-23

## 2026-08-11 — Stale watchdog alarms on stuck publish runs
- What: `digest-watchdog.yml` and `/api/cron-watchdog` now fail/flag when the digest is stale past the alarm window while `publish-digest.yml` is already in progress.
- Why: The old duplicate-dispatch guard exited green before the late alarm, so a hung publish could leave a stale edition with monitors reporting success.
- Gotcha: Early-window in-progress still skips duplicates; only the late stale state turns red.
