## 2026-08-12 — Block static deploys from applying stale incoming digest
- What: `publish-digest.yml` now skips `publish_digest.py` when the plan is static-only; added a workflow regression test for that guard.
- Why: a docs/API-only deploy with a leftover `incoming/new-main.html` could overwrite the live digest during an unrelated static publish.

## 2026-08-12 — Root archive runtime fetches + anchor archived scoreboards
- What: `mdSitePath()` resolves archive data fetches from the site root; archived scoreboards anchor to the edition date; assetVersion → 20260812 across live/admin/externalized archives.
- Why: archive pages fetched manifest/FRED/press reads under `/archive/` and later visits could score rows against real ET today instead of the archived edition.
- Redo of: 2026-07-22; 2026-07-23
