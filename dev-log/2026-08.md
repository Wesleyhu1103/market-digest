# Dev log — August 2026

## 2026-08-17 — Fix archive root data paths
- What: `mdSitePath()` now resolves root assets/data from archive pages; archive manifest and macro FRED loads use it, and asset-version sync updates archive snapshots.
- Why: externalized archives loaded JS/CSS but still requested `archive/archive/manifest.json` and archive-local `fred-data.json`, breaking archive nav and FRED-backed macro charts.
- Gotcha: JS/CSS changes must cache-bust existing archive snapshots, not just `index.html` and `admin.html`.
