## 2026-08-25 — Restore archive-safe runtime data paths
- What: shared archive/FRED/verdict fetches now route through `mdSitePath()`, archive asset cache-bust tags sync with `assetVersion`, and regression tests cover both.
- Why: externalized archive pages loaded JS/CSS but still fetched `/archive/archive/...` and `/archive/fred-data.json`, breaking archive nav, macro fallback charts, and verdict history.
- Gotcha: bump `docs/site-config.json` and run `python3 scripts/sync_site_config.py` after shared JS/CSS changes so archived snapshots pick up the new code.
