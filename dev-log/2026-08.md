# Dev log — August 2026

## 2026-08-20 — Root runtime fetches on archive pages
- What: `mdSitePath()` now roots archive manifest, FRED snapshot, and scoreboard archive fetches; assetVersion → 20260820 and sync covers externalized archives.
- Why: archive pages loaded `../js` but runtime fetches still looked under `/archive/archive/` and `/archive/fred-data.json`, breaking archive nav/charts/press reads.
- Redo of: 2026-07-23
- Gotcha: older pre-extraction archives still fail the hardcoded-date validator; the fixed externalized archives validate cleanly.
