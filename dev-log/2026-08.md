## 2026-08-18 — Root archive runtime fetches + archive cache-bust sync
- What: `mdSitePath()` now resolves archive manifest, FRED fallback, and verdict archive HTML from the site root; `sync_site_config.py` also bumps archive snapshot asset tags. assetVersion -> 20260818.
- Why: archived pages under `/archive/` were looking for `/archive/archive/manifest.json` and `/archive/fred-data.json`, breaking archive navigation and GitHub Pages archive macro data.
- Gotcha: archive snapshots need cache-bust tags updated too, or they keep loading stale shared JS.
