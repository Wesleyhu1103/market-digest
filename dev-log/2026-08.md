# Dev log — August 2026

## 2026-08-26 — Root archive runtime fetches + cache-bust snapshots
- What: `mdSitePath()` now roots archive manifest/FRED/archive HTML fetches from externalized pages; `sync_site_config.py` bumps archive snapshot `?v=` tags too. assetVersion → 20260826.
- Why: archived digests under `/archive/` were fetching `/archive/archive/manifest.json` and `/archive/fred-data.json`, breaking archive nav and static FRED-backed archive charts/scoreboards.
- Gotcha: changing shared JS for archives must update archived HTML query strings, not just `index.html`/`admin.html`.
