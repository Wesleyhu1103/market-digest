## 2026-08-15 — Block static deploys from applying stale incoming digest
- What: `publish-digest.yml` now skips `publish_digest.py` when the plan is static-only; added a workflow regression test for that guard.
- Why: a docs/API-only deploy with a leftover `incoming/new-main.html` could overwrite the live digest during an unrelated static publish.
